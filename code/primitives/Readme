————————————————————————————————————————
——        CS264A Final Project        ——
——  Alan Litteneker & Katherine Wang  ——
————————————————————————————————————————

This folder contains everything needed to
construct the libsat.a file for linking
with either a sat_solver or c2D build.

This uses the original, unmodified makefile
for building. To build, simply run:
    make